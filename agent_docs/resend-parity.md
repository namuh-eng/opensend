# OpenSend ↔ Resend parity matrix

Authoritative tracker for the parity-driven autonomous loop (`hermes-orchaestrator/skills/autonomous-ai-agents/opensend-orchestrator/references/parity-driven-loop.md`).

The parity-inspector reads this file each tick, picks the highest-priority unfiled gap, files a spec issue, and updates the row's `Issue #`. Manually-filed parity issues should also be backfilled into the relevant row in the same PR — otherwise the inspector will re-file.

**Columns**

- **Area** — broad category
- **Feature** — concrete capability
- **Resend** — short summary of how Resend ships it (cite docs URL when non-obvious)
- **OpenSend** — short summary of current behavior (cite `path:line` when applicable)
- **Feature gap** — `parity` / `partial` / `missing` / `lead`
- **DX gap** — API ergonomics, SDK quality, error messages, types, idempotency. `parity` / `behind` / `ahead`
- **Reliability gap** — SLO, multi-region, retry, replay, observability. `parity` / `behind` / `ahead`
- **Price gap** — what we'd charge vs Resend at equivalent volume. `cheaper` / `match` / `pricier` / `n/a`
- **Priority** — `P0` (closes a customer-blocking gap) / `P1` (visible parity) / `P2` (polish) / `needs Jaeyun` (architecture/pricing/infra decision)
- **Issue #** — populated by inspector when filed; empty = unfiled
- **Last reviewed** — UTC date the row was last verified against repo + Resend docs

Rows are ordered roughly by priority within each area. The inspector's tie-breaker is "smallest implementable slice first".

Key namespace note: OpenSend-issued keys use the `os_...` namespace. Resend-compatible means endpoint/body/response semantics; an `os_...` key prefix is not a parity blocker.

---

## Transactional API

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Single send (`POST /emails`) | `POST /emails` accepts `from`, `to`, `subject`, content, optional metadata (`cc`, `bcc`, `reply_to`, `headers`, `attachments`, `tags`, `scheduled_at`, `topic_id`, `template`) and `Idempotency-Key`; success returns `{ "id": "..." }`; duplicate idempotent retries within 24 hours return the same response without sending again, and the same key after 24 hours is accepted as new ([docs](https://resend.com/docs/api-reference/emails/send-email), [docs](https://resend.com/docs/dashboard/emails/idempotency-keys)) | parity in code/tests for the documented request/response core: middleware rewrites root `POST /emails` to the JSON API and leaves dashboard `GET /emails` alone (`src/middleware.ts`, `tests/e2e/emails-alias.spec.ts`); `POST /api/emails` accepts the Resend body fields and returns exactly `{ id }` on success (`src/lib/validation/emails.ts`, `src/lib/api/emails/send.ts`, `tests/api-emails.test.ts`); string/array recipients, `cc`/`bcc`/`reply_to`, scheduled ISO and natural-language `scheduled_at`, attachment `content`/`path`/`content_type`/`content_id`, and tag boundaries are covered locally (`tests/api-emails.test.ts`); `Idempotency-Key` now accepts up to 256 chars, single-send duplicate retries replay `{ id }` within 24 hours, and expired scoped keys are cleared before accepting a fresh request; live unauthenticated alias proof on 2026-05-12 confirmed `POST /emails` returns the JSON `missing_api_key` envelope while `GET /emails` remains the dashboard `/auth` flow on `https://opensend.namuh.co` and `https://api.opensend.namuh.co` ([evidence](live-validation/2026-05-12-issue-426-send-contract.md)); remaining gap is authenticated staging/prod proof because no deployed API key or safe test recipient was available locally | partial | parity | parity | n/a | P1 | #426 | 2026-05-12 |
| Batch send (`POST /emails/batch`) | `POST /emails/batch` accepts up to 100 email payloads and returns `{ "data": [{ "id": "..." }] }`; endpoint supports `Idempotency-Key`, with duplicate retries within 24 hours replaying the same accepted response and reuse after 24 hours accepted as new ([docs](https://resend.com/docs/api-reference/emails/send-batch-emails), [docs](https://resend.com/docs/dashboard/emails/idempotency-keys)) | partial: shared handler exists at `POST /api/emails/batch`, validates arrays up to 100 (`src/lib/validation/emails.ts`), checks API auth/idempotency (`src/lib/api/emails/batch-send.ts`), queues accepted rows, returns `{ data: [...] }`, replays duplicate accepted batch keys with the original `{ data: [...] }` envelope before quota, row, or queue side effects, and expires scoped keys after the 24-hour lookup window before accepting a fresh batch; remaining gap is fresh authenticated staging/prod proof after deployment | partial | parity | parity | n/a | P1 | #425 | 2026-05-12 |
| Scheduled send (`scheduled_at`) | `scheduled_at` accepts natural-language strings such as `in 1 min` and ISO 8601 timestamps; scheduled emails can later be updated with `PATCH /emails/:email_id` ([docs](https://resend.com/docs/api-reference/emails/send-email), [docs](https://resend.com/docs/dashboard/emails/schedule-email), [docs](https://resend.com/docs/api-reference/emails/update-email)) | partial: OpenSend accepts a `scheduled_at` string in send validation (`src/lib/validation/emails.ts:27`, `src/lib/validation/emails.ts:40`), persists future single and batch sends as `scheduled` (`src/app/api/emails/route.ts:339`, `src/app/api/emails/route.ts:487`, `src/app/api/emails/batch/route.ts:386`, `src/app/api/emails/batch/route.ts:414`), and worker scans enqueue due rows (`packages/core/src/db/repositories/emailRepo.ts:42`, `packages/ingester/src/queue-worker.ts:313`); gap: handlers use direct `new Date(...)` parsing for send/update (`src/app/api/emails/route.ts:339`, `src/app/api/emails/[id]/route.ts:90`), so Resend natural-language values like `in 1 min` are not normalized and invalid schedule strings are not rejected at validation boundary; Ever browser attempts failed with `Session expired` / `unknown command extract`, so official docs fallback was used on 2026-05-07 | partial | behind | behind | n/a | P1 | #240 | 2026-05-07 |
| Cancel scheduled send | `PATCH /emails/:id` | TBD | ? | ? | ? | ? | P2 | | |
| Idempotency keys | `Idempotency-Key` header on single and batch send; keys expire after 24 hours, max 256 chars, and same-key email requests return the same response without sending again ([docs](https://resend.com/docs/dashboard/emails/idempotency-keys)) | parity locally: single send validates/stores scoped keys, replays the accepted `{ id }` on duplicate retries within 24 hours, and accepts the same key as new after the window (`src/lib/api/emails/send.ts`, `tests/api-emails.test.ts`); batch send stores the accepted response envelope, replays `{ data: [...] }` for duplicate scoped keys before quota, row, or queue side effects within 24 hours, and accepts the same key as new after the window (`src/lib/api/emails/batch-send.ts`, `tests/api-emails.test.ts`, `tests/e2e/emails-alias.spec.ts`); OpenAPI documents `maxLength: 256` and the 24-hour window (`src/lib/openapi.ts`) | partial | parity | parity | n/a | P0 | #425 | 2026-05-12 |
| Reply-To list | Resend documents `replyTo` on `POST /emails` as `string | string[]`; Ever observed the live send-email docs on 2026-05-12 showing `replyTo`, `string | string[]`, and copy that multiple addresses should be sent as an array ([docs](https://resend.com/docs/api-reference/emails/send-email#body-parameters)) | partial: OpenSend REST contracts already accept `reply_to` as a string or array (`packages/core/src/contracts/send.ts:10`, `packages/core/src/contracts/send.ts:192`), single and batch handlers normalize and persist reply-to arrays (`src/lib/api/emails/send.ts:415`, `src/lib/api/emails/send.ts:622`, `src/lib/api/emails/batch-send.ts:553`, `src/lib/api/emails/batch-send.ts:575`), OpenAPI references the shared recipient schema for `reply_to` (`src/lib/openapi.ts:458`), and backend tests cover array reply-to persistence (`tests/api-emails.test.ts:1108`); gap: the TypeScript SDK send surface exposes snake_case `reply_to` through `EmailOptions` but has no Resend-style `replyTo?: string | string[]` alias or serialization test for `replyTo` arrays (`packages/sdk/src/index.ts:66`, `packages/core/src/dto/index.ts:27`, `tests/sdk-client.test.tsx`) | partial | behind | parity | n/a | P2 | #467 | 2026-05-12 |
| Attachments | `attachments[]` supports `content` as buffer/Base64 string, `filename`, hosted-file `path`, optional `content_type`, optional `content_id`, and a 40MB per-email encoded attachment limit ([docs](https://resend.com/docs/api-reference/emails/send-email#body-parameters)); docs fetch returned HTTP 200 on 2026-05-07; Ever degraded with `API server returned 502` and JSON parse failures after one retry | partial: OpenSend validates and stores `content`, `path`, `content_type`, and `content_id` attachment fields (`src/lib/validation/emails.ts:30`, `src/lib/email-attachments.ts:31`, `src/app/api/emails/route.ts:508`, `src/app/api/emails/batch/route.ts:435`); local `sendEmailSchema.safeParse` accepted a path-only attachment with `content_type`/`content_id` on 2026-05-07; gap: delivery normalization only forwards attachments with inline `content`, so path-only attachments are silently omitted (`packages/ingester/src/queue-worker.ts:554`), and SES MIME generation hardcodes `application/octet-stream` without `Content-ID` support (`src/lib/ses.ts:41`, `src/lib/ses.ts:242`) | partial | behind | behind | n/a | P1 | #250 | 2026-05-07 |
| Tags / metadata | `tags[]` objects identify emails for webhook/analytics filtering; docs constrain tag names/values to ASCII letters, numbers, underscores, or dashes, max 256 chars, and up to 75 tags per email ([docs](https://resend.com/docs/api-reference/emails/send-email#body-parameters), [docs](https://resend.com/docs/dashboard/emails/tags)) | partial: OpenSend validates `tags` as `{ name, value }[]` and persists them for single and batch sends (`src/lib/validation/emails.ts:12`, `src/lib/validation/emails.ts:124`, `src/app/api/emails/route.ts:484`, `src/app/api/emails/batch/route.ts:414`); email detail displays stored tags (`src/components/email-detail.tsx:378`); gap: validation allows spaces/punctuation/unicode, values up to 1024 chars, and has no 75-tag cap (`src/lib/validation/emails.ts:12`); local `sendEmailSchema.safeParse` accepted `bad tag!` / `ümlaut value with spaces` on 2026-05-07; Ever degraded after start/recovery with `Session expired` | partial | behind | parity | n/a | P1 | #243 | 2026-05-07 |

## Audiences / Broadcasts

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Audiences (lists) | deprecated but still-live Audiences API supports `POST /audiences`, `GET /audiences`, `GET /audiences/{id}`, and `DELETE /audiences/{id}`; Ever observed create docs showing `resend.audiences.create({ name: "Registered Users" })`, `POST /audiences`, response `{ object: "audience", id, name }`, and list docs showing `resend.audiences.list()`, `GET /audiences`, response `{ object: "list", has_more, data: [{ id, name, created_at }] }`; docs warn audiences are deprecated in favor of segments but endpoints still work ([create](https://resend.com/docs/api-reference/audiences/create-audience), [list](https://resend.com/docs/api-reference/audiences/list-audiences), [get](https://resend.com/docs/api-reference/audiences/get-audience), [delete](https://resend.com/docs/api-reference/audiences/delete-audience)) | partial: OpenSend has dashboard Audience tabs and a segment model/API (`packages/core/src/db/schema.ts:189`, `packages/core/src/services/audience-metadata.ts:261`, `packages/core/src/services/audience-metadata.ts:282`, `src/app/api/segments/route.ts:40`, `src/app/api/segments/route.ts:65`, `src/app/api/segments/[id]/route.ts:25`, `src/app/api/segments/[id]/route.ts:48`); prod `/audience/segments` showed `Create segment` and empty-state copy on 2026-05-11; prod `GET /api/segments` returned `401` JSON while `GET /api/audiences` returned `404` HTML; no root `src/app/audiences` adapter exists, unlike root contacts aliases (`src/app/contacts/route.ts:1`, `src/app/contacts/[contact_id]/route.ts:1`), and SDK has contacts but no audiences resource (`packages/sdk/src/index.ts:372`) | partial | behind | parity | n/a | P1 | #360 | 2026-05-11 |
| Contacts | CRUD plus unsubscribed state across create/list/retrieve/update/delete; docs show root `/contacts` endpoints and SDK methods, with retrieve/update/delete accepting either contact id or email ([docs](https://resend.com/docs/api-reference/contacts/create-contact), [docs](https://resend.com/docs/api-reference/contacts/list-contacts), [docs](https://resend.com/docs/api-reference/contacts/retrieve-contact), [docs](https://resend.com/docs/api-reference/contacts/update-contact), [docs](https://resend.com/docs/api-reference/contacts/delete-contact)); Ever degraded with `Unexpected token '<', "<!DOCTYPE "... is not valid JSON` on 2026-05-07 | partial: contact CRUD exists under `/api/contacts` (`src/app/api/contacts/route.ts:41`, `src/app/api/contacts/route.ts:160`, `src/app/api/contacts/[id]/route.ts:44`, `src/app/api/contacts/[id]/route.ts:95`, `src/app/api/contacts/[id]/route.ts:203`), validation accepts Resend-like fields (`src/lib/validation/contacts.ts:5`), detail lookup accepts UUID or email within tenant (`src/app/api/contacts/[id]/route.ts:28`), and lifecycle webhooks enqueue on create/update/delete (`src/app/api/contacts/route.ts:123`, `src/app/api/contacts/[id]/route.ts:176`, `src/app/api/contacts/[id]/route.ts:231`); gap: no root `/contacts` Resend-compatible surface, SDK points at `/api/contacts` and only has create/list/get methods (`packages/sdk/src/index.ts:368`) | partial | behind | parity | n/a | P1 | #256 | 2026-05-07 |
| Broadcasts | `resend.broadcasts.create(...)` creates draft/send-now/scheduled broadcasts, root `POST /broadcasts/:broadcast_id/send` sends or schedules API-created broadcasts, and the dashboard supports no-code/Markdown editor, testing/sending, bulk draft delete, bulk schedule cancel, clone-as-template, statuses, and export; Ever observed the docs/navigation and Node examples on 2026-05-11 ([intro](https://resend.com/docs/dashboard/broadcasts/introduction), [create](https://resend.com/docs/api-reference/broadcasts/create-broadcast), [send](https://resend.com/docs/api-reference/broadcasts/send-broadcast)) | partial: OpenSend has authenticated dashboard `/broadcasts` with empty state/create control observed via Ever on 2026-05-11, plus `/api/broadcasts` list/create/detail/update/delete/send routes (`src/app/api/broadcasts/route.ts:26`, `src/app/api/broadcasts/route.ts:54`, `src/app/api/broadcasts/[id]/route.ts:42`, `src/app/api/broadcasts/[id]/send/route.ts:12`) and a service that validates create/send status transitions (`packages/core/src/services/broadcast.ts:361`, `packages/core/src/services/broadcast.ts:429`); gap: prod root `/broadcasts` and `/broadcasts/test/send` returned `405` while `/api/broadcasts` reached API auth on 2026-05-11, docs still advertise `/api/broadcasts` (`src/app/docs/page.tsx:203`, `src/app/docs/page.tsx:207`), and the SDK has no broadcasts resource (`packages/sdk/src/index.ts:1`) | partial | behind | parity | n/a | P1 | #365 | 2026-05-11 |
| Segments | `resend.segments.create({ name })` maps to root `POST /segments` and returns `{ object: "segment", id, name }`; `resend.segments.list()` maps to `GET /segments` and returns `{ object: "list", has_more, data: [...] }`; Resend also documents `GET /segments/:segment_id`, `DELETE /segments/:segment_id`, and `GET /segments/:segment_id/contacts`; Ever observed create/list/delete docs and Node examples on 2026-05-12 ([create](https://resend.com/docs/api-reference/segments/create-segment), [list](https://resend.com/docs/api-reference/segments/list-segments), [retrieve](https://resend.com/docs/api-reference/segments/retrieve-segment), [delete](https://resend.com/docs/api-reference/segments/delete-segment)) | partial: OpenSend has segment list/create/detail/delete/contacts services under `/api/segments` (`src/app/api/segments/route.ts:40`, `src/app/api/segments/route.ts:65`, `src/app/api/segments/[id]/route.ts:25`, `src/app/api/segments/[id]/route.ts:48`, `src/app/api/segments/[id]/contacts/route.ts:28`), and prod `POST /api/segments` reaches API auth with a JSON missing-key response on 2026-05-12; gap: prod root `POST /segments`, `GET /segments/seg_test`, and `DELETE /segments/seg_test` redirect to `/auth`, Ever observed dashboard `/segments` as a 404 page, docs still advertise `/api/segments` (`src/app/docs/page.tsx:262`), and the SDK exposes no `segments` resource (`packages/sdk/src/index.ts:791`) | partial | behind | parity | n/a | P2 | #455 | 2026-05-12 |
| Unsubscribe management | hosted page + List-Unsubscribe header ([docs](https://resend.com/docs/dashboard/emails/add-unsubscribe-to-transactional-emails)); broadcasts auto-handle `{{{RESEND_UNSUBSCRIBE_URL}}}` ([docs](https://resend.com/docs/dashboard/broadcasts/introduction)) | partial: hosted `/unsubscribe/[contactId]` page calls auth-protected contact PATCH (`src/app/unsubscribe/[contactId]/page.tsx:17`, `src/app/api/contacts/[id]/route.ts:70`); send APIs only persist caller headers (`src/app/api/emails/route.ts:293`, `src/app/api/emails/batch/route.ts:176`); broadcast footer placeholder is not wired to a generated URL (`src/components/broadcast-editor.tsx:97`, `src/components/broadcast-editor.tsx:1310`) | partial | behind | behind | n/a | P0 | #173 | 2026-05-03 |

## Domains

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Custom domain add | `POST /domains` returns copyable DNS records and supports default `send` plus custom return-path labels ([docs](https://resend.com/docs/api-reference/domains/create-domain), [docs](https://resend.com/docs/dashboard/domains/introduction#custom-return-path)) | partial: create API provisions SES identity and returns records (`src/app/api/domains/route.ts:46`, `src/app/api/domains/route.ts:58`); verify endpoint exists (`src/app/api/domains/[id]/verify/route.ts:16`); service persists `customReturnPath` but record generation ignores it and emits SPF/MX at the root domain (`packages/core/src/services/domain.ts:86`, `packages/core/src/services/domain.ts:174`, `packages/core/src/services/domain.ts:190`); Cloudflare auto-configure also checks/creates root-domain SPF/MX (`src/lib/cloudflare.ts:147`, `src/lib/cloudflare.ts:158`) | partial | behind | behind | n/a | P0 | #185 | 2026-05-04 |
| DKIM/SPF/DMARC | guided DKIM/SPF/DMARC DNS setup; DMARC docs recommend `_dmarc` TXT starter policy `v=DMARC1; p=none;` ([docs](https://resend.com/docs/dashboard/domains/introduction), [docs](https://resend.com/docs/dashboard/domains/dmarc)) | partial: domain creation emits DKIM CNAME plus return-path SPF/MX records but no DMARC TXT record (`packages/core/src/services/domain.ts:102`, `packages/core/src/services/domain.ts:121`, `packages/core/src/services/domain.ts:129`); Cloudflare auto-configure creates DKIM/SPF/MX and warns on existing SPF/MX but has no `_dmarc` handling (`src/lib/cloudflare.ts:154`, `src/lib/cloudflare.ts:176`, `src/lib/cloudflare.ts:189`, `src/lib/cloudflare.ts:219`); dashboard DNS grouping only shows DKIM and SPF/sending buckets (`src/components/domain-detail.tsx:627`, `src/components/domain-detail.tsx:677`, `src/components/domain-detail.tsx:686`) | partial | behind | behind | n/a | P0 | #188 | 2026-05-04 |
| Multi-region domain | EU + US-East + others | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| Subdomain delegation (CNAME) | Resend recommends verified sending subdomains such as `updates.example.com` to isolate reputation, and `POST /domains` supports `tracking_subdomain` so a value such as `links` on `example.com` produces a CNAME record for `links.example.com` ([docs](https://resend.com/docs/dashboard/domains/introduction), [docs](https://resend.com/docs/api-reference/domains/create-domain)); Ever observed the domains docs navigation/recommendation on 2026-05-12, but `ever extract` is unavailable in this CLI (`unknown command extract`), so exact parameter wording used official `.md` docs fallback | partial: OpenSend validates and persists `tracking_subdomain` on create/update (`src/lib/validation/domains.ts:43`, `src/lib/validation/domains.ts:52`, `src/app/api/domains/route.ts:107`, `packages/core/src/services/domain.ts:289`), but DNS generation emits DKIM CNAMEs plus return-path SPF/MX/DMARC only and never adds a tracking CNAME (`packages/core/src/services/domain.ts:147`, `packages/core/src/services/domain.ts:167`, `packages/core/src/services/domain.ts:174`, `packages/core/src/services/domain.ts:204`); Cloudflare helpers can create CNAMEs generically (`src/lib/cloudflare.ts:3`, `src/lib/cloudflare.ts:55`) but no tracking-specific auto-configure path is wired; Ever observed prod `/domains` Add domain on 2026-05-12 showing only a domain input and no advanced tracking-subdomain control | partial | behind | behind | n/a | P2 | #470 | 2026-05-12 |

## Webhooks

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Event types (sent/delivered/opened/clicked/bounced/complained) | full set: docs list `email.sent`, `email.delivered`, `email.delivery_delayed`, `email.complained`, `email.bounced`, `email.opened`, `email.clicked`, `email.failed`, `email.scheduled`, `email.suppressed`, `email.received`, `domain.created`, `domain.updated`, `domain.deleted`, `contact.created`, `contact.updated`, and `contact.deleted` ([docs](https://resend.com/docs/dashboard/webhooks/event-types), [docs](https://resend.com/docs/api-reference/webhooks/create-webhook)); fetched docs returned HTTP 200 and those event names on 2026-05-06; Ever browser start failed with `Extension not connected` for both Resend docs and OpenSend prod | partial: shared validation accepts the email lifecycle subset `email.sent`, `email.delivered`, `email.bounced`, `email.complained`, `email.delivery_delayed`, `email.opened`, `email.clicked`, and `email.failed` (`packages/core/src/webhook-events.ts:1`, `src/lib/validation/webhooks.ts:9`); SES/SNS normalization maps send/delivery/bounce/complaint/delay/open/click/failure events into stored email events and webhook dispatch (`packages/ingester/src/ses-event-normalization.ts:1`, `packages/ingester/src/index.ts:295`, `packages/ingester/src/index.ts:340`); direct schema observation rejects Resend lifecycle names `contact.created`, `domain.created`, and `email.scheduled` with `Unsupported webhook event type` on 2026-05-06; contact/domain CRUD routes do not enqueue dispatchable webhook deliveries (`src/app/api/contacts/route.ts:23`, `src/app/api/contacts/[id]/route.ts:71`, `src/app/api/domains/route.ts:28`, `src/lib/events.ts:44`) | partial | behind | behind | n/a | P0 | #218 | 2026-05-06 |
| Signing (Svix-compatible HMAC SHA256 headers) | Resend webhook requests include `svix-id`, `svix-timestamp`, and `svix-signature`; signing secrets are verified with HMAC SHA256 ([docs](https://resend.com/docs/webhooks/verify-webhooks-requests)) | parity: dispatcher signs outbound webhook payloads with a Svix-compatible HMAC (`packages/ingester/src/dispatcher.ts:88`, `packages/ingester/src/dispatcher.ts:100`); signing helper strips `whsec_` and returns `v1,<base64>` (`packages/core/src/webhook-signing.ts:7`, `packages/core/src/webhook-signing.ts:14`); tests assert signed headers are sent (`tests/webhook-dispatcher.test.ts:86`, `tests/webhook-dispatcher.test.ts:140`) | parity | parity | parity | n/a | P2 | n/a | 2026-05-06 |
| Retries with backoff | retry schedule is immediate, then 5 seconds, 5 minutes, 30 minutes, 2 hours, 5 hours, 10 hours, and 10 hours; webhook message details show the next retry time ([docs](https://resend.com/docs/webhooks/retries-and-replays)) | partial: dispatcher persists attempts and `nextRetryAt` for failed responses/errors (`packages/ingester/src/dispatcher.ts:108`, `packages/ingester/src/dispatcher.ts:126`), scans due pending deliveries (`packages/ingester/src/dispatcher.ts:147`), and worker routes `webhook.dispatch` jobs through the dispatcher (`packages/ingester/src/queue-worker.ts:283`); default ladder is `[10s, 60s, 5m, 30m, 2h, 6h, 24h]`, not Resend's cadence (`packages/ingester/src/dispatcher.ts:9`, `tests/webhook-dispatcher.test.ts:256`) | partial | parity | behind | n/a | P0 | #221 | 2026-05-06 |
| Replay from dashboard | Dashboard flow can replay webhook messages from Webhooks → endpoint → message → **Replay**; docs explicitly allow replaying both failed and succeeded messages, observed via Ever on 2026-05-11 ([docs](https://resend.com/docs/webhooks/retries-and-replays)) | partial: OpenSend stores webhook delivery rows and API detail can return `recent_deliveries` (`packages/core/src/services/webhook.ts:207`, `src/lib/api/webhooks/handlers.ts:59`), but prod `/webhooks` only showed endpoint/status/events/created list columns and empty-state copy on 2026-05-11; dashboard maps only webhook metadata (`src/app/(dashboard)/webhooks/page.tsx:19`) and `WebhooksList` renders no delivery/message detail or replay action (`src/components/webhooks-list.tsx:50`); no replay route exists under `src/app/api/webhooks/[id]` | partial | behind | behind | n/a | P1 | #375 | 2026-05-11 |
| Per-event filtering at endpoint | yes | TBD | ? | ? | ? | ? | P2 | | |

## Templates

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| React Email integration | first-class React Email DX in framework/template guides: Resend docs show sending React components through the Node SDK with `react: EmailTemplate(...)` / `react: <Email />`, and the React Email knowledge-base flow scaffolds `@react-email/components` before sending via Resend ([Next.js](https://resend.com/docs/send-with-nextjs), [React Email guide](https://resend.com/docs/knowledge-base/template-emails-with-react-email)); Ever observed the React send example and docs navigation on 2026-05-11 | partial: OpenSend SDK documents a React component example (`packages/sdk/README.md:66`) and has a client-side `react` render path (`packages/sdk/src/index.ts:65`, `packages/sdk/src/index.ts:206`, `packages/sdk/src/index.ts:276`), but the public type is `react?: unknown`, render failures are silently ignored before the API validates only `html`/`text`/`template` (`src/lib/validation/emails.ts:187`, `src/lib/validation/emails.ts:204`), and the SDK package declares no React/ReactDOM peer or optional dependency contract (`packages/sdk/package.json:20`); prod `/docs` showed API reference sections without React Email setup on 2026-05-11 | partial | behind | parity | n/a | P1 | #372 | 2026-05-11 |
| Variable interpolation | Templates support up to 50 custom variables with `key`, `type` (`string`/`number`), optional `fallbackValue`, reserved names, triple-brace placeholders like `{{{PRODUCT}}}`, and send-time fallback rendering when variables are omitted ([docs](https://resend.com/docs/dashboard/templates/template-variables), [docs](https://resend.com/docs/api-reference/templates/create-template)); Ever observed the docs and code examples on 2026-05-09 | partial: OpenSend stores template variables only as `{ name, required }` (`src/lib/db/schema.ts:330`), create/update collapse input to that shape (`src/app/api/templates/route.ts:147`, `src/app/api/templates/[id]/route.ts:117`), detail responses force `type: "string"` and `fallback_value: null` (`src/app/api/templates/[id]/route.ts:45`, `src/app/api/templates/[id]/route.ts:152`), and send-time rendering replaces only double-brace placeholders from provided values (`src/lib/api/emails/send.ts:493`, `src/lib/api/emails/send.ts:533`); OpenSend prod `/templates` showed the dashboard empty state but the captured create click did not expose variable controls on 2026-05-09 | partial | behind | parity | n/a | P1 | #294 | 2026-05-09 |
| Stored templates (server-side render) | yes | TBD | ? | ? | ? | ? | P2 | | |

## Analytics

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Delivery stats | Broadcast performance docs expose delivered, unsubscribed, click rate, and open rate after send, observed via Ever on 2026-05-11 ([docs](https://resend.com/docs/dashboard/broadcasts/performance-tracking)); tags docs describe key/value tags that associate emails with application data and are included in webhook events, observed via Ever on 2026-05-11 ([docs](https://resend.com/docs/dashboard/emails/tags)) | partial: OpenSend production `/metrics` showed `All Domains`, `Last 15 days`, `DELIVERABILITY RATE`, `All Events`, `BOUNCE RATE`, and `COMPLAIN RATE` on 2026-05-11, and no tag filter; repo has dashboard metrics by range/domain/event type (`src/app/api/metrics/route.ts:35`, `src/app/api/metrics/route.ts:55`, `src/components/metrics-page.tsx:146`, `src/components/metrics-page.tsx:200`), aggregate SQL covers total/delivered/bounce/complaint plus domain breakdown (`packages/core/src/services/dashboardAggregates.ts:203`, `packages/core/src/services/dashboardAggregates.ts:264`, `packages/core/src/services/dashboardAggregates.ts:357`), and the deliverability UI exposes an event dropdown (`src/components/deliverability-section.tsx:20`, `src/components/deliverability-section.tsx:269`); gap: stored email tags are not exposed as metric options or accepted as metrics filters | partial | behind | parity | n/a | P1 | #378 | 2026-05-11 |
| Open / click tracking | disabled by default; dashboard and API can enable `tracking_subdomain` with `click_tracking` and/or `open_tracking`; verified tracking subdomain is used for tracked links, click tracking redirects through Resend, and open tracking inserts a 1x1 pixel; Ever observed docs/API tab on 2026-05-11 ([docs](https://resend.com/docs/dashboard/domains/tracking)) | partial: OpenSend persists domain flags and exposes dashboard toggles (`packages/core/src/db/schema.ts:85`, `packages/core/src/db/schema.ts:94`, `packages/core/src/services/domain.ts:290`, `src/components/domain-detail.tsx:766`, `src/components/domain-detail.tsx:799`); prod `/domains/{id}` Configuration showed Click/Open Tracking switches on 2026-05-11, but Records showed only sending/DMARC DNS and no tracking CNAME; gap: send flow stores rendered HTML after template/unsubscribe handling (`src/lib/api/emails/send.ts:564`, `src/lib/api/emails/send.ts:585`) and the worker forwards `email.html`/`email.text` directly to SES without link rewrite or open pixel injection (`packages/ingester/src/queue-worker.ts:429`, `packages/ingester/src/queue-worker.ts:433`); repo search found no OpenSend click/open tracking routes, even though SES normalization can map provider Open/Click events (`packages/ingester/src/ses-event-normalization.ts:11`, `packages/ingester/src/ses-event-normalization.ts:12`) | partial | behind | behind | n/a | P1 | #382 | 2026-05-11 |
| Bounce / complaint feed | Resend Emails dashboard exposes row-level status filters for `Bounced`, `Complained`, and `Suppressed`; Resend docs describe bounce types and suppression details caused by bounced/complained recipients ([email bounces](https://resend.com/docs/dashboard/emails/email-bounces), [email suppressions](https://resend.com/docs/dashboard/emails/email-suppressions)); Ever observed the dashboard status menu and docs on 2026-05-11 | partial: OpenSend production `/metrics` showed aggregate `BOUNCE RATE` and `COMPLAIN RATE`, and `/emails` showed `Bounced`, `Complained`, and `Suppressed` status filters plus an Export button on 2026-05-11; repo has suppression storage/API (`src/lib/db/schema.ts:229`, `src/app/api/suppressions/route.ts:12`, `packages/core/src/services/suppressions.ts:88`) and dashboard email status filtering (`src/app/(dashboard)/emails/page.tsx:33`, `src/components/emails-sending-filter-bar.tsx:33`), but the Emails Export button is a no-op (`src/components/emails-sending-filter-bar.tsx:115`) and the unused export modal fetches API-key-only `/api/emails` from a dashboard context (`src/components/export-modal.tsx:75`, `src/app/api/emails/route.ts:16`); gap: no reachable dashboard export/feed for bounce, complaint, and suppression rows | partial | behind | behind | n/a | P1 | #385 | 2026-05-11 |
| CSV export | dashboard CSV export is available for Emails, Broadcasts, Contacts, Segments, Domains, Logs, and API keys; admins can export filtered data, <=1,000 rows download immediately, larger exports are delivered by email, and exports remain accessible for 7 days under Settings → Team → Exports; Ever observed this on 2026-05-12 ([docs](https://resend.com/docs/dashboard/api-keys/introduction)) | partial: OpenSend has one session-authenticated delivery-failure CSV endpoint (`src/app/api/dashboard/delivery-failures/export/route.ts`) wired from the Emails filter bar (`src/components/emails-sending-filter-bar.tsx:170`); Logs and API keys build client-side CSV only from currently loaded rows (`src/components/logs-list-page.tsx:103`, `src/components/api-keys-list.tsx:194`); Domains, Contacts, and Broadcasts render export controls with no shared export action (`src/components/domains-page.tsx:146`, `src/components/contacts-list.tsx:149`, `src/components/broadcasts-list.tsx:545`); no `/api/exports` route or dashboard Exports page exists (`find src/app -path "*export*"` returned only delivery-failures export on 2026-05-12) | partial | behind | behind | n/a | P2 | #461 | 2026-05-12 |

## API & SDKs

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| OpenAPI spec | published as OpenAPI 3.0.3 YAML/JSON in `resend/resend-openapi` (`https://raw.githubusercontent.com/resend/resend-openapi/main/resend.yaml` returned HTTP 200 with `openapi: 3.0.3` on 2026-05-06) | missing: no OpenAPI/Swagger artifact, dependency, or script in `package.json`; docs are a hand-authored client page (`src/app/docs/page.tsx:17`, `src/app/docs/page.tsx:21`); prod `GET /openapi.json` redirected to `/auth` and `GET /api/openapi.json` returned 404 on 2026-05-06; Ever browser evidence degraded because `ever start --url ... --title` failed with `unknown option --title` and retry failed with `Extension not connected` | missing | behind | parity | n/a | P1 | #214 | 2026-05-06 |
| Node SDK | first-class Node.js SDK with `import { Resend } from "resend"`, `new Resend(apiKey)`, `emails.send(...)`, and built-in TypeScript declarations (official Resend Node/send-email docs and npm package verified 2026-05-06; Ever browser evidence degraded because sessions expired immediately after start) | partial: SDK package exists and publishes declarations (`packages/sdk/package.json:2`, `packages/sdk/package.json:6`), but exports `Opensend` rather than `Resend` (`packages/sdk/src/index.ts:460`, `packages/sdk/src/index.ts:484`), requires explicit `baseUrl` (`packages/sdk/src/index.ts:26`, `packages/sdk/src/index.ts:103`, `tests/sdk-client.test.tsx:9`), and sends through `/api/emails` plus `/api/emails/batch` instead of root Resend-compatible aliases (`packages/sdk/src/index.ts:238`, `packages/sdk/src/index.ts:250`) | partial | behind | parity | n/a | P0 | #227 | 2026-05-06 |
| Python SDK | first-class Python SDK with `import resend`, `resend.api_key = os.environ["RESEND_API_KEY"]`, typed `resend.Emails.SendParams`, `resend.Emails.send(params)`, `resend.Emails.send_batch(params)`, and async variants in the full package ([docs](https://resend.com/docs/send-with-python), [repo](https://github.com/resend/resend-python)) | partial: first-party package exists at `packages/python-sdk/pyproject.toml` and `packages/python-sdk/src/opensend/__init__.py`; it provides module-level `opensend.api_key`, `opensend.Emails.send(...)`, `opensend.Emails.send_batch(...)`, `OpenSend`/`Resend` instance clients, typed send params/results, root `/emails` and `/emails/batch` calls, configurable `base_url`, and error envelope exceptions; docs live at `packages/python-sdk/README.md` and `docs/sdk/python.md`; tests cover send, batch, auth/base URL, success, and API errors in `packages/python-sdk/tests/test_emails.py`; gap: first slice intentionally omits full resource parity and async support | partial | behind | parity | n/a | P1 | #264 | 2026-05-08 |
| Go SDK | first-class Go SDK install uses `go get github.com/resend/resend-go/v3`; quickstart creates `resend.NewClient("re_xxxxxxxxx")`, builds `resend.SendEmailRequest`, calls `client.Emails.Send(params)`, and reads `sent.Id`; docs also link Go examples for attachments, inline images, templates, scheduling, audiences, domains, inbound webhooks, double opt-in, Chi, and Gin ([docs](https://resend.com/docs/send-with-go)); Ever observed install/send/example-card shape on 2026-05-11 | missing: no first-party Go client SDK package or Go SDK docs; `package.json` workspaces include `packages/*` and `services/*` (`package.json:6`), Python SDK exists as a client package (`packages/python-sdk/pyproject.toml:5`), but the only Go module is the experimental shadow ingester service and explicitly not a client SDK (`services/ingester-go/README.md:1`, `services/ingester-go/README.md:5`); prod docs showed API reference + MCP setup with no Go SDK quickstart on 2026-05-11 | missing | behind | parity | n/a | P1 | #356 | 2026-05-11 |
| Ruby / PHP / .NET / Rust SDKs | first-party language SDK quickstarts/packages exist for Ruby (`gem install resend`, `require "resend"`, `Resend.api_key "re_xxxxxxxxx"`) and PHP (`composer require resend/resend-php`), and official Resend docs also publish .NET and Rust send quickstarts; Ever observed the Ruby and PHP install/send docs on 2026-05-12 ([Ruby](https://resend.com/docs/send-with-ruby), [PHP](https://resend.com/docs/send-with-php), [.NET](https://resend.com/docs/send-with-dotnet), [Rust](https://resend.com/docs/send-with-rust)) | missing: OpenSend has first-party TypeScript and Python SDK packages (`packages/sdk/package.json`, `packages/python-sdk/pyproject.toml`), but no Ruby/PHP/.NET/Rust client package manifests were found under `packages/` (`*.gemspec`, `composer.json`, `*.csproj`, or `Cargo.toml`), and repo docs expose no Ruby/PHP/.NET/Rust SDK quickstarts as of 2026-05-12 | missing | behind | parity | n/a | P2 | #464 | 2026-05-12 |
| MCP server | official MCP server supports stdio via `npx -y resend-mcp`, Streamable HTTP with `--http --port 3000`, and an authenticated `/mcp` endpoint using `Authorization: Bearer re_xxxxxxxxx`; docs list coverage for emails, received emails, contacts, broadcasts, domains, segments, topics, contact properties, API keys, and webhooks ([docs](https://resend.com/docs/knowledge-base/mcp-server), [changelog](https://resend.com/changelog/mcp)); Ever degraded with `API server returned 502` / JSON parse failures after one recovery attempt on 2026-05-07, so official docs fallback was used | missing: no MCP dependency/package/script is present (`package.json:7`, `package.json:30`); standalone Hono control-plane service currently exposes only `/healthz` and `/readyz` (`services/api/src/index.ts:19`, `services/api/src/index.ts:22`, `services/api/src/index.ts:30`); docs page is a static API reference without MCP setup (`src/app/docs/page.tsx:17`, `src/app/docs/page.tsx:344`); local dev was not running, prod `/mcp` redirected to `/auth`, and prod `/api/mcp` returned 404 on 2026-05-07 | missing | behind | parity | n/a | P1 | #260 | 2026-05-07 |
| TypeScript types | strict, exported public SDK declarations; Resend npm `6.12.3` publishes `types: ./dist/index.d.mts` and exports many request/response aliases including `CreateEmailOptions`, `CreateEmailRequestOptions`, `CreateEmailResponse`, `ListEmailsResponse`, and `ErrorResponse` (registry tarball inspected 2026-05-06) | partial: current SDK package points consumers at generated declarations (`packages/sdk/package.json:5`, `packages/sdk/package.json:6`, `packages/sdk/package.json:7`), exports `Resend`/`Opensend` plus common request/response DTOs (`packages/sdk/src/index.ts:504`, `packages/sdk/src/index.ts:507`), and documents type imports (`packages/sdk/README.md:196`); still trails Resend's breadth of exported aliases for every resource | partial | behind | parity | n/a | P0 | #227 | 2026-05-06 |

## DX

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Sandbox / test mode | test-recipient addresses simulate delivery outcomes without damaging reputation: `delivered@resend.dev`, `bounced@resend.dev`, `complained@resend.dev`, and `suppressed@resend.dev`; delivered/bounced/complained support plus-labeling such as `delivered+signup@resend.dev`; send examples use `Acme <onboarding@resend.dev>` ([docs](https://resend.com/docs/dashboard/emails/send-test-emails), [docs](https://resend.com/docs/api-reference/emails/send-email)); docs fetched HTTP 200 on 2026-05-07; Ever degraded after `API server returned 502`/JSON parse errors and one recovery attempt | parity slice: exact Resend test-recipient detection lives in `packages/core/src/services/sandboxTestRecipients.ts`; single and batch send APIs reject `suppressed@resend.dev` with the public `recipient_suppressed` envelope before quota/rows/queue while preserving per-item batch results (`src/app/api/emails/route.ts`, `src/app/api/emails/batch/route.ts`); accepted delivered/bounced/complained rows keep normal `{ id }` / `{ data: [...] }` response shapes and queue `email.send`; the worker detects all-`to` sandbox recipients, avoids SES, records durable sent/delivered/bounced/complained events, refreshes bounce/complaint suppressions, and enqueues matching tenant webhook deliveries (`packages/ingester/src/queue-worker.ts`) | parity | parity | parity | n/a | P1 | #253 | 2026-05-08 |
| Error message quality | documented error catalog with stable names and suggested actions ([docs](https://resend.com/docs/api-reference/errors)) | partial: send routes return ad hoc string errors/details (`src/app/api/emails/route.ts:93`, `src/app/api/emails/batch/route.ts:102`); SDK exposes only `message`/`statusCode` (`packages/sdk/src/index.ts:30`) | partial | behind | parity | n/a | P0 | #170 | 2026-05-03 |
| Logs / event explorer | searchable per email | parity: send APIs capture sanitized tenant-scoped request logs for accepted/failed authenticated sends (`src/app/api/emails/route.ts`, `src/app/api/emails/batch/route.ts`, `src/lib/api-logging.ts`); logs list/detail APIs are tenant-scoped and searchable/filterable (`src/app/api/logs/route.ts`, `src/app/api/logs/[id]/route.ts`); dashboard logs search and email-associated request logs are wired (`src/app/(dashboard)/logs/page.tsx`, `src/components/logs-list-page.tsx`, `src/app/(dashboard)/emails/[id]/page.tsx`, `src/components/email-detail.tsx`) | parity | parity | parity | n/a | P0 | #224 | 2026-05-07 |
| Dashboard quality | Emails dashboard first-run empty state acts as onboarding: Ever observed authenticated Resend `/emails` on 2026-05-11 showing `No sent emails yet`, helper copy `Start sending emails to see insights and previews for every message.`, and a visible `Go to docs` CTA | partial: Ever observed authenticated OpenSend prod `/emails` on 2026-05-11 showing Sending filters and only `No emails found`; the docs link exists in the header overflow menu (`src/components/emails-header.tsx:96`) rather than the empty state, and the table empty branch renders only the generic copy (`src/components/emails-sending-data-table.tsx:81`); page composition currently passes filtered rows into the table (`src/components/emails-sending-page.tsx:110`) after the server page loads the row list (`src/app/(dashboard)/emails/page.tsx:54`) | partial | behind | parity | n/a | P1 | #432 | 2026-05-11 |
| API key scopes | `Full access` can create, delete, get, and update any resource; `Sending access` can only send emails and may be restricted to a domain ([docs](https://resend.com/docs/dashboard/api-keys/introduction)); docs fetched HTTP 200 on 2026-05-07; Ever browser degraded after two `Session expired` failures | partial: OpenSend stores `full_access` / `sending_access` and lets users choose a domain when creating keys (`src/lib/db/schema.ts:99`, `packages/core/src/services/apiKeys.ts:8`, `src/components/api-keys-list.tsx:313`); API key management itself requires `full_access` (`src/app/api/api-keys/route.ts:49`, `src/app/api/api-keys/[id]/route.ts:23`); gap: resource APIs such as domains, contacts, and webhooks accept any valid API key without enforcing send-only restrictions (`src/app/api/domains/route.ts:53`, `src/app/api/contacts/route.ts:40`, `src/app/api/webhooks/route.ts:14`), and send routes do not compare `auth.domain` to the request `from` domain (`src/lib/api-auth.ts:100`, `src/app/api/emails/route.ts:206`, `src/app/api/emails/batch/route.ts:208`) | partial | behind | behind | n/a | P1 | #247 | 2026-05-07 |

## Reliability

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Public SLO | implied via status page | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| Multi-region send | EU/US/AP | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| Status page | public status page shows overall operational copy, component uptime/status, subscribe/report actions, and incident history; Ever observed `We’re fully operational`, `Subscribe to updates`, `View history`, and component rows for Email Sending, SMTP, General API, Email Events, Webhooks, Dashboard, and Website at `status.resend.com` on 2026-05-11 | missing: prod `/status` redirected to `/auth` and `/api/status` returned 404 HTML on 2026-05-11; only low-level health exists at `/api/health`, returning `{ "status": "ok", "db": "connected" }`, backed by `src/app/api/health/route.ts:14` and `packages/core/src/services/health.ts:31`; `/metrics` is dashboard-auth gated (`src/app/api/metrics/route.ts:36`, `src/app/api/metrics/route.ts:38`) and is not a public status page | missing | behind | behind | n/a | P1 | #388 | 2026-05-11 |
| Queue under provider degradation | Resend send docs return an email id for accepted sends and dashboard docs expose user-visible states including `queued`, `sent`, `failed`, and `delivery_delayed` ([docs](https://resend.com/docs/api-reference/emails/send-email), [docs](https://resend.com/docs/dashboard/emails/introduction)); Ever browser attempts failed with `Session expired` / `Session not found`, so static docs fallback was used on 2026-05-06 | partial: API routes persist queued email rows then publish `email.send` jobs (`src/app/api/emails/route.ts:408`, `src/app/api/emails/route.ts:455`, `src/app/api/emails/batch/route.ts:330`, `src/app/api/emails/batch/route.ts:386`); worker marks `processing`, calls SES, marks `sent`, and on provider error resets status to `queued` before rethrowing for SQS redelivery (`packages/ingester/src/queue-worker.ts:366`, `packages/ingester/src/queue-worker.ts:373`, `packages/ingester/src/queue-worker.ts:387`, `packages/ingester/src/queue-worker.ts:396`, `packages/ingester/src/queue-worker.ts:398`); SQS receive count/backoff is metric/log-only and there is no per-email retry/dead-letter state (`packages/ingester/src/queue-worker.ts:187`, `packages/ingester/src/queue-worker.ts:490`, `packages/core/src/db/schema.ts:120`, `packages/core/src/db/schema.ts:132`) | partial | parity | behind | n/a | P0 | #235 | 2026-05-06 |
| Send-time observability (per-message trace) | Email detail exposes message metadata, a documented event taxonomy (`bounced`, `canceled`, `clicked`, `complained`, `delivered`, `delivery_delayed`, `failed`, `opened`), associated logs, and export/share actions; Ever observed the docs sections on 2026-05-11 ([docs](https://resend.com/docs/dashboard/emails/introduction)) | partial: OpenSend dashboard detail fetches tenant-scoped email rows, events, and associated logs (`src/app/(dashboard)/emails/[id]/page.tsx:19`, `src/app/(dashboard)/emails/[id]/page.tsx:29`, `src/app/(dashboard)/emails/[id]/page.tsx:35`), but maps events to only `{ type, timestamp }` before rendering a simple label/time list (`src/app/(dashboard)/emails/[id]/page.tsx:81`, `src/components/email-detail.tsx:419`); richer event evidence already exists in `email_events.source_id`/`payload` and the lifecycle events API (`src/lib/db/schema.ts:422`, `src/lib/db/schema.ts:424`, `packages/core/src/services/emailLifecycle.ts:48`, `src/app/api/emails/[id]/events/route.ts:22`), while the current email-detail Playwright proof is skipped/conditional (`tests/e2e/email-detail.spec.ts:2`, `tests/e2e/email-detail.spec.ts:21`); OpenSend prod `/emails` was reachable via Ever but showed `No emails found` for the authenticated account on 2026-05-11 | partial | behind | behind | n/a | P1 | #391 | 2026-05-11 |

## Compliance / security

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| SOC2 | yes | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| GDPR / EU data residency | yes | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| Audit log | yes | TBD | ? | ? | ? | ? | P1 | #394 | 2026-05-11 |
| Suppression list management | suppresses recipients after hard bounce or spam complaint; suppression details are visible from email detail and can be removed ([docs](https://resend.com/docs/dashboard/emails/email-suppressions)) | missing: no suppression table/API/dashboard route (`https://opensend.namuh.co/api/suppressions` returned 404 on 2026-05-05); contacts only store manual unsubscribe state (`src/lib/db/schema.ts:192`, `src/lib/db/schema.ts:199`); send routes queue recipients without suppression lookup (`src/app/api/emails/route.ts:344`, `src/app/api/emails/route.ts:358`, `src/app/api/emails/batch/route.ts:264`, `src/app/api/emails/batch/route.ts:278`); SES bounce/complaint events update email status but do not create suppressions (`packages/ingester/src/ses-event-normalization.ts:5`, `packages/ingester/src/ses-event-normalization.ts:6`, `packages/core/src/db/repositories/emailEventRepo.ts:5`) | missing | behind | behind | n/a | P0 | #191 | 2026-05-05 |

## Pricing

| Feature | Resend | OpenSend | Feature | DX | Reliability | Price | Priority | Issue # | Last reviewed |
|---|---|---|---|---|---|---|---|---|---|
| Free tier | 3K/mo, 100/day | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| Paid tiers | $20/50K → scaled | TBD | ? | ? | ? | ? | needs Jaeyun | | |
| Enterprise | custom | TBD | ? | ? | ? | ? | needs Jaeyun | | |

---

## Maintenance

- **Inspector tick** auto-updates `Issue #` and `Last reviewed` when it files.
- **Manual sweeps** are needed when columns drift (Resend ships something new, OpenSend ships outside the loop). Update the affected row in the same PR.
- **`needs Jaeyun` rows** are skipped by the inspector — sweep them with Jaeyun periodically; either downgrade to a P-tier with concrete scope, or accept they stay parked.
- **`TBD` cells** in the OpenSend column are the inspector's first-pass job: fill them by reading the repo before filing the issue. The first few inspector ticks will mostly be matrix-fill, not issue-filing — that's expected.
