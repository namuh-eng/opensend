# Send emails with SMTP

SMTP integration status.

OpenSend is primarily REST/API-first in this repository. SMTP compatibility should be treated as a separate feature surface unless enabled in your deployment.
