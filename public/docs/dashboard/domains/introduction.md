# Managing Domains

Domain verification and DNS setup.

OpenSend uses DNS records for DKIM, SPF, DMARC, return-path, and tracking setup. Operators can copy records manually or use supported DNS automation.
