# Custom Headers

Attach custom headers to outbound email.

Use the `headers` field on send payloads for provider-safe custom headers. Do not put secrets or sensitive internal identifiers in email headers.
